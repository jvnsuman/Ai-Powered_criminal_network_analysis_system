"""
api/routes/

Route modules for the API: ingestion.py, query.py, evidence.py.
Each should expose a `router` object (FastAPI) or blueprint (Flask)
that api/main.py registers.

Status: [TODO] — Dec P1, not started.
"""
