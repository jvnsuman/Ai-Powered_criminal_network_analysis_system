"""
api/routes/query.py

Retrieve case graph/evidence for the dashboard.

Status: [TODO] — Dec P1, not started.
"""


def query_endpoint(entity_or_case_id: str):
    """[TODO] Retrieve the graph (or subgraph) and metadata for a given
    entity or case, scoped by the requesting user's role
    (schema.user.get_user_cases).
    """
    raise NotImplementedError
