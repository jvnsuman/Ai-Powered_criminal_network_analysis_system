/**
 * dashboard/src/components/GraphCanvas.jsx
 *
 * Interactive network graph canvas using Cytoscape.js. Minimum required
 * interactions per project notes Section 12:
 * - click a node -> update EvidencePanel
 * - hover -> tooltip (name, relationship type, weight)
 * - zoom and pan
 * - filter/search integration with SearchBar
 * - expand/collapse dense subgraphs
 *
 * Status: [TODO] — Dec P1, not started.
 */

export default function GraphCanvas({ graphData, onNodeSelect }) {
  // TODO: mount Cytoscape.js instance, bind interaction handlers
  throw new Error("GraphCanvas not implemented");
}
