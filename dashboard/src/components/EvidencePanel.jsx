/**
 * dashboard/src/components/EvidencePanel.jsx
 *
 * Right-panel evidence trail for the selected node — source documents,
 * confidence scores. Fetches from api/routes/evidence.py.
 *
 * Status: [TODO] — Dec P1, not started.
 */

export default function EvidencePanel({ selectedEntityId }) {
  // TODO: fetch evidence trail via api/routes/evidence.py, render list
  throw new Error("EvidencePanel not implemented");
}
