/**
 * dashboard/src/components/CaseSummaryCards.jsx
 *
 * Top-of-dashboard summary metric cards: entities linked, key
 * influencers count, flagged patterns count.
 *
 * Status: [TODO] — Dec P1, not started.
 */

export default function CaseSummaryCards({ caseStats }) {
  // TODO: render three metric cards from caseStats
  throw new Error("CaseSummaryCards not implemented");
}
