/**
 * dashboard/src/components/SearchBar.jsx
 *
 * Filter/isolate graph nodes by name, phone, or alias.
 *
 * Status: [TODO] — Dec P1, not started.
 */

export default function SearchBar({ onSearch }) {
  // TODO: input + debounce, calls onSearch(query) which highlights/
  // isolates matching nodes in GraphCanvas
  throw new Error("SearchBar not implemented");
}
